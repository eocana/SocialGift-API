const router = require('../daos/whishlist.dao.js');

const {createWishlists, addGiftToWishlist} = require('../user.controller.js');

router.get('/users', all);
//router.post('/create', authMiddleware, create);